import { Player } from "../player"
import { Node } from "../../node"
import { Settings } from "../../settings"
import { Team } from "src/team"

describe("Player", () => {
  test("it should have certain properties", () => {
    // actual test
  })
})
